---
import LinkButton from './LinkButton.astro';

interface Props {
    title: string;
    description: string;
    date: string;
    link?: string;
}
const { title, description, date, link } = Astro.props;
---

<div
    class="absolute w-3 h-3 bg-orange-400 dark:bg-yellow-200 rounded-full mt-1.5 -start-1.5 text-yellow-200"
>
</div>
<time
    class="p-0 m-0 text-sm text-gray-600/80 dark:text-white/80"
    >{date}</time
>
<h3 class="text-lg font-semibold text-orange-400 dark:text-yellow-200/90 mt-4 mb-2">
    {title}
</h3>
<p class="mb-4 text-base font-normal text-gray-600 dark:text-gray-300 text-pretty">
    {description}
</p>
{
    link && <LinkButton href={link}>
    Saber más ...{" "}
            <svg
                class="w-3 h-3 ms-2 rtl:rotate-180"
                aria-hidden="true"
                fill="none"
                viewBox="0 0 14 10"
            >
                <path
                    stroke="currentColor"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M1 5h12m0 0L9 1m4 4L9 9"
                />
            </svg>
    </LinkButton>
    
}
